import { AddButton, Provider } from "@ui-business/AddReminder";

const AddReminderButton = () => {
  return (
    <Provider>
      <AddButton size="small" />
    </Provider>
  );
};

export default AddReminderButton;
